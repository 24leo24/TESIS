# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['producto_tesis.py'],
    pathex=[],
    binaries=[],
    datas=[('./img/*.ico', 'img'),('./img/*.gif', 'img') ,('./database/contenedores.db', 'database'),('./usuario/*.py', 'usuario')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='producto_tesis',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='producto_tesis',
)
